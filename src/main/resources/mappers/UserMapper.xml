<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.UserMapper">

    <select id="getAnyUser" resultType="com.example.pojo.SsUser">
        SELECT *
        FROM ss_user
            LIMIT 1
    </select>
    
    <select id="userVerify" resultType="com.example.pojo.SsUser">
        SELECT *
        FROM ss_user
        WHERE username=#{username} AND password=#{password}
    </select>


</mapper>